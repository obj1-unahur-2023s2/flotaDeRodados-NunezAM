import wollok.game.*
import vehiculos.*
program corsasEnElGame {
	// hacer las inicializaciones necesarias 
	var corsa = new Corsa(color="rojo", position=game.at(5,6), image="autitorojo.png")
	game.width(15)
	game.height(15)
	game.addVisualCharacter(corsa)
	game.start()
} 